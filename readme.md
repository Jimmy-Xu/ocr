# dependency
```
pip3 install easyocr

# auto install the following lib:
PyYAML, opencv-python-headless, Pillow, torch, torchvision, PyWavelets, tifffile, networkx, cycler, pyparsing, kiwisolver, matplotlib, imageio, scikit-image, python-bidi, easyocr
```

# usage
```
$ python3 main.py
```
